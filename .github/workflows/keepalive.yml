name: Supabase keepalive

# Gratis Supabase-projecten pauzeren na ~1 week zonder verkeer.
# Deze ping om de 3 dagen houdt het project wakker.
on:
  schedule:
    - cron: '17 3 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -sf "https://ogqytxojtnddzsjidyjf.supabase.co/rest/v1/catan_shared?select=id&limit=1" \
            -H "apikey: sb_publishable_bo2NeFurx39ErHGee1gfLw_xyYc81yi" \
            -H "Authorization: Bearer sb_publishable_bo2NeFurx39ErHGee1gfLw_xyYc81yi" \
            -o /dev/null
